# GuGeeGoo API（预留）

当前阶段 **不需要后端**：规划在 `学习档案/`，分发靠 `stage:deploy` + COS。

后续可在此放置 **Cloudflare Workers**：

- 为家长生成 COS 预签名 URL（答案 PDF、`private/planning`）
- 健康检查、Webhook

配置模板见 [`../../infra/cloudflare/wrangler.toml.example`](../../infra/cloudflare/wrangler.toml.example)。
