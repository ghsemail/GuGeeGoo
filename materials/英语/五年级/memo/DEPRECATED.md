# 已弃用脚本

`generate-pdf.mjs` 使用内联 HTML 生成复习备忘 PDF，已被统一的 PDF 生成器取代。

## 新的生成方式

```bash
npm run pdf -- materials/英语/五年级/memo/<文件名>.md --type memo
```

## 保留原因

此脚本暂时保留以便查阅原有实现（卡片式布局）。未来可能将卡片布局整合到统一生成器中。
