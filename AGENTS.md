# AGENTS.md — 项目协作约定

> 本文档供 AI 助手 / 自动化代理阅读，定义了维护此学习项目的核心约定。

---

## 1. 项目概述

此仓库是为五年级学生**顾景源**设计的家长主导学习项目。

- **英语**：五年级上册（外研版，外研五上）
- **数学**：学而思五年级秋季班

家长规划与批改，AI 助手协助录入知识点、生成练习与追踪薄弱点。

---

## 2. 目录结构

```
GuGeeGoo/
├── 学习档案/                         # 家长端规划与追踪
│   ├── 学习路线图.md                 # 学习路线图（总览，双学科）
│   ├── 学习进度.md                   # 进度记录（按日期，双学科）
│   ├── 英语-水平档案.md              # 英语薄弱点档案
│   ├── 英语-错题本.md                # 英语错题本
│   ├── 数学-水平档案.md              # 数学薄弱点档案
│   └── 数学-错题本.md                # 数学错题本
│
├── 学科学习/                         # 学习材料
│   ├── 英语/五年级/
│   │   ├── 讲义/                     # 知识点 MD
│   │   └── 练习/                     # 练习（按单元分文件夹）
│   │       ├── 第N单元/              # 单元 PDF
│   │       │   └── 源文件/           # 单元源文件（MD）
│   │       └── 脚本/                 # PDF 生成脚本
│   └── 数学/五年级/学而思五年级秋/
│       ├── 讲义/                     # 讲次知识点 MD
│       ├── 练习/                     # 练习 PDF
│       │   └── 源文件/               # 练习 MD 源文件
│       └── 备忘/                     # 复习备忘
│
├── apps/                             # 网站代码（与学习 MD 分离）
│   ├── web/                          # 学生主页（Cloudflare Pages）
│   └── api/                          # 预留：Cloudflare Workers
├── infra/                            # Cloudflare / 腾讯云 COS 配置说明
├── deploy/staging/                   # stage:deploy 快照（含 private/planning）
├── assets/images/                    # 插图、讲义扫描件
├── scripts/                          # PDF、stage-deploy、sync-cos
├── 老师/                             # AI 教师配置（各学科子文件夹）
│   ├── 数学/SOUL.md                  # 数学老师身份与原则
│   └── 数学/MEMORY.md                # 数学老师当前状态（每次工作后更新）
├── AGENTS.md                         # 本文件
└── README.md                         # 项目说明
```

---

## 3. ID 编号方案

| 学科 | 薄弱点 ID | 已掌握 ID | 下一个可用 |
|------|-----------|-----------|-----------|
| 英语 | WP-001, WP-002, … | MP-001, MP-002, … | WP-007, MP-009 |
| 数学 | WP-M001, WP-M002, … | MP-M001, MP-M002, … | WP-M005, MP-M002 |

**规则**

- **WP** = Weak Point（薄弱点）
- **MP** = Mastered Point（已掌握）
- 英语 ID 为三位数字（如 WP-005）
- 数学 ID 带 `M` 前缀后接三位数字（如 WP-M001）

---

## 4. 掌握规则

1. **连续 2 次全对** → 状态改为「已掌握」（WP → MP）
2. **做错 1 次** → 连续正确计数归零
3. **已掌握再做错** → 回退为「薄弱」（MP → WP，同一 ID 或新建）

> 在 profile 的「当前关注」表格中用 `连续正确` 列追踪（如 `1/2`）。

---

## 5. 批改后同步流程（四档同步）

家长批改后，AI 助手需依次更新以下 4 个文件（按学科区分错题本）：

| 顺序 | 文件 | 操作 |
|------|------|------|
| 1 | `英语-错题本.md` / `数学-错题本.md` | 添加每道错题一行 |
| 2 | `英语-水平档案.md` / `数学-水平档案.md` | 更新「当前关注」连续正确计数；若达标则移至「已掌握」并记录巩固日志；新错题则添加薄弱点明细 |
| 3 | `学习进度.md` | 添加一行含 **成绩** 加粗（如 **7/9**），带学科标记 [英]/[数] |
| 4 | `学习路线图.md` | 更新对应单元 / 讲次状态及近期计划勾选 |

> **注意**：`学习进度.md` 和 `学习路线图.md` 是双学科合并文件，用学科标记或分节区分。

### 错题本格式（英语 / 数学通用）

```markdown
| 日期 | 来源 | 题号 | 题目 | 景源作答 | 正确写法 | 知识点 | 备注 |
|------|------|------|------|----------|----------|--------|------|
| 2026-09-25 | Unit 2 Day 2 · 选词填空 | Q2 | … clean small __________. | children | **places** | WP-005 | 课文原句 |
```

---

## 6. 材料输出格式

- **MD + PDF**，练习材料**绝不输出 HTML**（`apps/web` 主页除外，且不得展示 WP/MP 编码）
- 练习 PDF：A4、大字号（17–20px）、每日一页 7–10 题
- 小测 PDF：5 题，字号更大（20px+）
- 答案 PDF：合订为家长专用（`...-answers.pdf`）
- 复习备忘 PDF：单页卡片式

### 学生可见内容规范

**学生看到的练习/答案文件（MD 源文件和 PDF）禁止包含：**

- 薄弱点编码（WP-001、WP-M001 等）
- 已掌握编码（MP-001、MP-M001 等）
- 内部标签（如题型标签、日期引用「Day 3 Q9」等）
- 内部序号（打印标题不显示「练习 1」「练习 2」等，用「巩固练习」代替）

**编码仅保留在规划文件中：**

- `学习档案/` 目录下的所有文件（错题本、水平档案、学习进度、学习路线图）
- 规划文件可记录练习针对哪些编码，但练习本身不显示

### 练习格式规范

- **副标题行布局**：学生信息在左（如「顾景源 · 五年级数学 · 学而思秋季班」），日期填写栏在右（「日期：________」），同一行显示，无需单独姓名栏
- **答案页面**：无日期栏
- **打印标题格式**：「第 X 讲 主题名 巩固练习」（题目）、「第 X 讲 主题名 巩固练习 答案」（答案）
- **计算空间**：练习题留空白供计算，不加「计算过程」「验算」等标签
- 源文件与 PDF **同名（中文）**，仅扩展名不同；打印标题不显示内部序号

### PDF 生成命令

```bash
# 在仓库根目录执行
npm run pdf -- <练习MD路径> [--answers <答案MD路径>] [--out <输出目录>]
```

- 不带 `--answers` 时仅生成练习 PDF
- 带 `--answers` 时同时生成答案 PDF
- 输出默认到同目录 `../`（即 `exercises/`）

---

## 7. 练习生成工作流

1. **草稿**：创建 `第N单元/源文件/第N单元-*.md` 或 `第N讲-*.md` 及 `-答案.md`（英语按单元分文件夹；数学仍用 `练习/源文件/`）
2. **家长确认**：在 `学习路线图.md` 或练习索引标注「待家长确认」
3. **生成 PDF**：确认后执行 `npm run pdf`
4. **批改 → 四档同步**（见 §5）

> 在家长确认前可先生成 PDF 预览，但需明确标注「草稿」状态。

---

## 8. 文件命名约定

**原则**：Markdown 源文件与配套 PDF **同名（中文）**，仅扩展名不同（`.md` / `.pdf`）。合订 PDF（如四天巩固）对应多份分日 MD 或单独 `-答案.md`。

### 英语

| 类型 | 模式 | 示例 |
|------|------|------|
| 单元索引（讲义） | `五年级上-单元索引.md` | — |
| 知识点 | `第<N>单元-<主题>.md` | `第2单元-绿色生活.md` |
| 四天巩固（分日） | `第<N>单元-四天巩固-第<D>天.md` | `第2单元-四天巩固-第1天.md` |
| 四天巩固 PDF | `第<N>单元-四天巩固.pdf` | 与合订练习同名 |
| 答案 | `...-答案.md` / `...-答案.pdf` | `第2单元-四天巩固-答案.pdf` |
| 小测 / 薄弱点 / Memo | `第<N>单元-<类型>.md` | `第1单元-单元小测.md`、`第1单元-复习备忘.md` |
| 练习索引 | `第<N>单元-练习索引.md` | — |

### 数学（学而思）

| 类型 | 模式 | 示例 |
|------|------|------|
| 课程索引 | `课程索引.md` | — |
| 知识点 | `第<N>讲.md` | `第1讲.md` |
| 回顾训练 | `第<N>讲-回顾训练.md` | `第1讲-回顾训练.pdf` |
| 复习备忘 | `第<N>讲-复习备忘.md` | 位于 `备忘/` |
| 答案 | `...-答案.md` | `第1讲-回顾训练-答案.pdf` |

---

## 9. 练习 MD 格式

```markdown
# <标题>

> <学生信息> · <学科>  
> **针对：** <知识点列表>  
> **打印版：** `<pdf文件名>`

---

## 一、<题型>

*题目说明（斜体）*

1. 题目内容 __________(填空用下划线)  <!-- [知识点ID] -->

## 二、<题型>

…
```

- 每题末尾可用 HTML 注释标注知识点：`<!-- [WP-M001] -->`
- 填空用 10–20 个下划线 `__________`

---

## 10. Web 与 COS 部署

| 命令 | 作用 |
|------|------|
| `npm run stage:deploy` | `学习档案/*.md` → `deploy/staging/private/planning/`；公开 PDF → `deploy/staging/public/` |
| `npm run sync:cos -- --dry-run` | 列出将上传 COS 的对象 |
| `npm run sync:cos` | 上传 staging（需 `infra/cos/env.example` 中的密钥） |
| `npm run web:build` | 构建学生主页 → `apps/web/dist/` |

- **学生主页**不得打包 `学习档案/`；规划 MD 只走 COS **`private/planning/`**（私有 ACL）。
- 四档同步更新 `学习档案/` 后，启动前或发布前执行 **`npm run stage:deploy`**（可选再 `sync:cos`）。
- 说明文档：`infra/README.md`、`apps/web/README.md`。

---

## 11. Git 约定

1. **直接在 main 分支工作**，不使用功能分支，不创建 PR
2. **家长的 Obsidian obsidian-git 每 ~10 分钟自动提交推送**，因此提交前务必同步
3. **提交推送流程**：
   - 提交前先执行 `git pull --rebase origin main`
   - 然后 `git add` + `git commit` + `git push origin main`
   - 若 push 被拒绝（远程有新提交），再次 `git pull --rebase origin main` 后重试
4. **禁止 force push**（`--force` / `-f`）

---

## 12. 周循环工作流

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  Intake     │ ──► │  练习生成   │ ──► │  批改同步   │
│ (新知识点/  │     │ (MD → 确认  │     │ (四档同步   │
│  错题录入)  │     │  → PDF)     │     │  → 下轮计划)│
└─────────────┘     └─────────────┘     └─────────────┘
        ▲                                      │
        └──────────────────────────────────────┘
```

### Intake（录入）

- 按学校 / 学而思进度录入新课知识点
- 家长上传错题照片后录入对应学科的错题本（`英语-错题本.md` 或 `数学-错题本.md`）

### 练习生成

- 针对当前薄弱点（`WP-*`）出题
- 每日 7–10 题，覆盖多个知识点
- 小测 5 题，聚焦 1–2 个薄弱点

### 批改同步

- 四档同步（见 §5）
- 更新学习路线图计划勾选

---

## 13. 数学符号渲染

练习 / 答案 MD 中的数学符号：

- **比号**：直接用冒号 `3:4`
- **分数**：用斜杠 `3/4` 或文字 `四分之三`
- **连比**：`A : B : C`

PDF 生成器会正确渲染中文冒号与比号。

---

## 14. 快速检查清单

开始新任务前：

- [ ] 执行 `git pull --rebase origin main` 同步最新代码
- [ ] 读取 `学习档案/学习路线图.md` 了解近期计划
- [ ] 读取对应 `英语-水平档案.md` / `数学-水平档案.md` 了解薄弱点

完成任务前：

- [ ] 四档同步（如有批改）
- [ ] 新练习已在学习路线图/练习索引标注「待家长确认」
- [ ] PDF 已生成（或注明草稿）
- [ ] `git pull --rebase origin main` → `git add` → `git commit` → `git push origin main`

---

*最后更新：2026-09-26*
